% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal_functions.enrichment.R
\name{.get.enrichment.table}
\alias{.get.enrichment.table}
\title{.get.enrichment.table}
\usage{
.get.enrichment.table(enrichment, name = NA, string.pattern.to.remove = NULL)
}
\arguments{
\item{enrichment}{An object of class \code{DEprot.enrichResult} (DEprot), \code{enrichResult} or \code{gseaResult} (clusterProfiler), or a data.frame with the same structure of the ones returned by clusterProfiler.}

\item{name}{String indicating the name of the discovery, used to fill the 'discovery' column. Default: \code{NA}.}

\item{string.pattern.to.remove}{String with a regular expression of a pattern to be removed from the geneset names when generating the 'alias' column. Default: \code{NULL} (no changes).}
}
\value{
A data.frame with the columns: discovery, enrichment.type, ID, alias, Count, set.size, GeneRatio.numeric, FoldEnrichment, NES, pvalue, p.adjust. \code{NULL} when the enrichment is empty or of an unsupported class.
}
\description{
Internal. Extracts the results of a single enrichment discovery and returns them with a common set of column names, independently of the tool and of the type of analyses used to generate them. GSEA and ORA results do not share the same columns: the leading edge of a GSEA is used as the equivalent of the ORA 'Count', and the size of the geneset as its background.
}
\keyword{internal}
