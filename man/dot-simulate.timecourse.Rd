% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timecourse.R
\name{.simulate.timecourse}
\alias{.simulate.timecourse}
\title{.simulate.timecourse}
\usage{
.simulate.timecourse(
  n.proteins = 1000,
  timepoints = c(0, 1, 2, 6, 24),
  n.replicates = 3,
  groups = NULL,
  fraction.responsive = 0.3,
  amplitude.range = c(0.8, 3),
  baseline.mean = 22,
  baseline.sd = 2,
  noise.sd = 0.25,
  n.genesets.per.archetype = 2,
  geneset.size = 40,
  geneset.purity = 0.6,
  na.fraction = 0,
  seed = 1234
)
}
\arguments{
\item{n.proteins}{Numeric value indicating the number of proteins to simulate. Default: \code{1000}.}

\item{timepoints}{Numeric vector of the timepoints of the design. Default: \code{c(0, 1, 2, 6, 24)}.}

\item{n.replicates}{Numeric value indicating the number of replicates per timepoint. Default: \code{3}.}

\item{groups}{Character vector indicating the group levels to simulate (e.g., treatment and control). When more than one group is provided, only the first one shows the temporal response, the others staying flat. Default: \code{NULL} (a single group).}

\item{fraction.responsive}{Numeric value indicating the fraction of proteins showing a temporal trend. Default: \code{0.3}.}

\item{amplitude.range}{Numeric vector of two elements indicating the range of the response amplitudes, in log2 units. Default: \code{c(0.8, 3)}.}

\item{baseline.mean}{Numeric value indicating the mean log2 intensity of the proteins. Default: \code{22}.}

\item{baseline.sd}{Numeric value indicating the standard deviation of the baseline log2 intensities. Default: \code{2}.}

\item{noise.sd}{Numeric value indicating the standard deviation of the technical noise added to each measure. Default: \code{0.25}.}

\item{n.genesets.per.archetype}{Numeric value indicating how many genesets are generated for each kinetic archetype. Default: \code{2}.}

\item{geneset.size}{Numeric value indicating the number of members of each geneset. Default: \code{40}.}

\item{geneset.purity}{Numeric value between 0 and 1 indicating the fraction of the members of a geneset that are drawn from the corresponding archetype, the rest being drawn at random. Default: \code{0.6}.}

\item{na.fraction}{Numeric value indicating the fraction of measures to be replaced by \code{NA}. Default: \code{0}.}

\item{seed}{Numeric value indicating the seed used for the simulation. Default: \code{1234}.}
}
\value{
A list with four elements: \code{counts} (matrix of log2 intensities), \code{metadata} (data.frame with the columns column.id, time.hours, replicate, group and combined.id), \code{truth} (data.frame associating each protein to its archetype and amplitude) and \code{TERM2GENE} (data.frame with the columns gs_name and gene_symbol).
}
\description{
Generates a small synthetic time-course proteomics dataset, used by the vignette and by the unit tests. A fraction of the proteins follows one of five kinetic archetypes (monotone up, monotone down, early transient, late transient, early drop with rebound), the remaining ones being flat plus noise. A matching geneset annotation is produced as well, each geneset being enriched in the proteins of one archetype, so that the over-representation analyses have something to recover.
}
\keyword{internal}
