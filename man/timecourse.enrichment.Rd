% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timecourse.R
\name{timecourse.enrichment}
\alias{timecourse.enrichment}
\title{timecourse.enrichment}
\usage{
timecourse.enrichment(
  DEprot.timecourse.object,
  TERM2GENE,
  clusters = NULL,
  top.n = NULL,
  universe = NULL,
  gsub.pattern.prot.id = NULL,
  min.cluster.size = 5,
  pvalueCutoff = 0.05,
  qvalueCutoff = 0.05,
  pAdjustMethod = "BH",
  dotplot.n = 5,
  size.by = "FoldEnrichment",
  size.range = c(5, 14),
  number.size = 2.7,
  show.numbers = TRUE,
  max.term.length = 60,
  viridis.option = "rocket"
)
}
\arguments{
\item{DEprot.timecourse.object}{An object of class \code{DEprot.timecourse}.}

\item{TERM2GENE}{Data.frame containing two columns 'gs_name' (IDs of the gene sets) and 'gene_symbol' (indicating the gene IDs). No default.}

\item{clusters}{Numeric value (or vector) indicating the cluster(s) to analyze. Default: \code{NULL} (all the clusters).}

\item{top.n}{Numeric value indicating how many best-ranked proteins of each cluster should be used for the enrichment. Default: \code{NULL} (all the proteins of the cluster).}

\item{universe}{Character vector indicating the background gene list. Default: \code{NULL}, meaning that all the proteins tested by \code{analyze.timecourse} are used. This is almost always the correct background for a proteomics experiment, since only the quantified proteins could have been detected as trending.}

\item{gsub.pattern.prot.id}{String indicating a pattern to be passed to gsub and to remove from the prot.id. Default: \code{NULL} (no changes in the IDs).}

\item{min.cluster.size}{Numeric value indicating the minimal number of proteins required to run the enrichment on a cluster. Default: \code{5}.}

\item{pvalueCutoff}{Numeric value indicating the adjusted pvalue cutoff on enrichment tests to report. Default: \code{0.05}.}

\item{qvalueCutoff}{Numeric value indicating the qvalue cutoff on enrichment tests to report as significant. Default: \code{0.05}.}

\item{pAdjustMethod}{String indicating the method to use for the p-value adjustment. One among "holm", "hochberg", "hommel", "bonferroni", "BH", "BY", "fdr", "none". Default: \code{"BH"}.}

\item{dotplot.n}{Numeric value indicating the maximum number of genesets displayed per cluster in the dotplot. Default: \code{5}.}

\item{size.by}{String indicating the metric mapped on the size of the dots. One among: 'FoldEnrichment' or 'GeneRatio'. Default: \code{"FoldEnrichment"}.}

\item{size.range}{Numeric vector of two elements indicating the minimal and maximal size of the dots. They must stay large enough for the numbers to fit inside. Default: \code{c(5, 14)}.}

\item{number.size}{Numeric value indicating the font size of the count displayed inside the dots. Default: \code{2.7}.}

\item{show.numbers}{Logic value indicating whether the number of proteins should be written inside the dots. Default: \code{TRUE}.}

\item{max.term.length}{Numeric value indicating the maximal number of characters of the geneset names displayed on the y-axis (longer names are truncated). Default: \code{60}.}

\item{viridis.option}{String indicating the viridis palette used for the significance color scale. Default: \code{"rocket"}.}
}
\value{
An object of class \code{DEprot.timecourse.enrichment}.
}
\description{
Performs an OverRepresentation Analysis (ORA) independently for each cluster of a time-course analysis, using as background (universe) all the proteins that were tested. It returns the combined results and a dotplot in which the size of each dot reflects the enrichment, the color its significance, and the number written inside the dot the count of proteins found in that geneset.
}
\seealso{
\code{\link{analyze.timecourse}}, \code{\link{heatmap.timecourse}}, \code{\link{geneset.enrichment}}
}
\author{
Sebastian Gregoricchio
}
