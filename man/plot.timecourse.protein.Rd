% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timecourse.R
\name{plot.timecourse.protein}
\alias{plot.timecourse.protein}
\title{plot.timecourse.protein}
\usage{
plot.timecourse.protein(
  DEprot.timecourse.object,
  protein.id,
  values = "counts",
  reference.time = NULL,
  color = "black",
  group.colors = NULL,
  show.points = TRUE,
  show.fit = TRUE,
  shape.column = NULL,
  log.x = FALSE,
  show.stats = TRUE,
  panel.border = TRUE,
  ncol = NULL,
  line.size = 0.8,
  point.size = 2,
  scale.expression = NULL
)
}
\arguments{
\item{DEprot.timecourse.object}{An object of class \code{DEprot.timecourse}.}

\item{protein.id}{String (or vector of strings) indicating the proteins to plot. The identifiers must correspond to the full row.names of the counts table.}

\item{values}{String indicating the quantity displayed on the y-axis. One among: 'counts' (the log2 values as they are), 'log2FC' (log2 fold change relative to \code{reference.time}; the counts being already in log2, this is a simple subtraction), 'zscore' (Z-score of the measured values, the fitted curve being centered/scaled with the same mean and SD so that curve and points remain comparable). Aliases are accepted ('fc', 'foldchange', 'log2', 'z', ...). Default: \code{"counts"}.}

\item{reference.time}{Numeric value indicating the timepoint used as baseline when \code{values} is 'log2FC'. Default: \code{NULL} (the earliest timepoint).}

\item{color}{String indicating the color of the line and of the points. Used only when no \code{group.column} was defined in the analyses; with several groups use \code{group.colors} instead. Default: \code{"black"}.}

\item{group.colors}{Named vector of colors to use for the groups. Default: \code{NULL} (automatic).}

\item{show.points}{Logic value indicating whether the individual samples should be displayed. Default: \code{TRUE}.}

\item{show.fit}{Logic value indicating whether the fitted trajectory should be displayed. Default: \code{TRUE}.}

\item{shape.column}{String indicating a column from the metadata table to use as factor for the shape of the points (e.g., 'replicate'). The column name is used as title of the shape legend. Default: \code{NULL}.}

\item{log.x}{Logic value indicating whether the x-axis should use a log10(time + 1) scale, useful with log-spaced timepoints. Default: \code{FALSE}.}

\item{show.stats}{Logic value indicating whether padj, amplitude, shape, cluster and rank should be added as subtitle (single protein only). Default: \code{TRUE}.}

\item{panel.border}{Logic value indicating whether a border should be drawn around each panel. Default: \code{TRUE}.}

\item{ncol}{Numeric value indicating the number of columns of the facet grid when several proteins are plotted. Default: \code{NULL} (automatic).}

\item{line.size}{Numeric value indicating the thickness of the fitted line. Default: \code{0.8}.}

\item{point.size}{Numeric value indicating the size of the mean points. Default: \code{2}.}

\item{scale.expression}{Deprecated, kept for backward compatibility: \code{TRUE} is equivalent to \code{values = "zscore"}, \code{FALSE} to \code{values = "counts"}. Default: \code{NULL}.}
}
\value{
A plot of class ggplot2, facetted by protein when several proteins are provided.
}
\description{
Plots the temporal trend of one or more individual proteins: the measured values (one point per sample), the mean +/- SEM at each timepoint, and the trajectory fitted by \code{analyze.timecourse}.
}
\seealso{
\code{\link{analyze.timecourse}}, \code{\link{plot.timecourse.profiles}}
}
\author{
Sebastian Gregoricchio
}
