% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sPLS.DA_plots.R
\name{plot.sPLSDA.performance}
\alias{plot.sPLSDA.performance}
\title{plot.sPLSDA.performance}
\usage{
plot.sPLSDA.performance(
  DEprot.sPLSDA.object,
  metric = "both",
  distance = "all",
  line.colors = NULL,
  title = "**Cross-validated classification error**"
)
}
\arguments{
\item{DEprot.sPLSDA.object}{An object of class \code{DEprot.sPLSDA}.}

\item{metric}{String indicating which error to display. One among: 'both', 'overall', 'BER' (balanced error rate). Default: \code{"both"}.}

\item{distance}{String indicating the prediction distance(s) to display. One among: 'all', 'max.dist', 'centroids.dist', 'mahalanobis.dist'. Default: \code{"all"}.}

\item{line.colors}{Color-vector used for the distances. Default: \code{NULL} (automatic colors).}

\item{title}{String indicating the title of the plot (markdown annotation supported). Default: \code{"**Cross-validated classification error**"}.}
}
\value{
A ggplot object.
}
\description{
Plots the cross-validated classification error of a sPLS-DA as a function of the number of components.
}
\details{
The balanced error rate is the average of the error rates of each class, and it is the value to read on an unbalanced design: with 9 samples in one group and 3 in the other, a model that assigns everything to the larger group already reaches an overall error of 25%.

The dashed gray line marks the error of a random assignment, that is one minus the frequency of the largest class. A model sitting on that line is not discriminating anything, however clean its scatter plot looks.
}
\examples{
\donttest{
# the performances are estimated by resampling the samples
splsda <- perform.sPLSDA(DEprot.object = DEprot::test.toolbox$dpo.imp,
                         group.column = "condition",
                         keepX = 5,
                         validate = TRUE,
                         folds = 3,
                         nrepeat = 1)

plot.sPLSDA.performance(DEprot.sPLSDA.object = splsda)
}

}
\seealso{
\link{perform.sPLSDA}, \link{plot.sPLSDA.tuning}, \link{plot.sPLSDA.auroc}
}
\author{
Sebastian Gregoricchio
}
