% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sPLS.DA_plots.R
\name{plot.sPLSDA.cumulative}
\alias{plot.sPLSDA.cumulative}
\title{plot.sPLSDA.cumulative}
\usage{
plot.sPLSDA.cumulative(
  DEprot.sPLSDA.object,
  bar.color = "steelblue",
  line.color = "navyblue",
  title = NULL
)
}
\arguments{
\item{DEprot.sPLSDA.object}{An object of class \code{DEprot.sPLSDA}.}

\item{bar.color}{String indicating the color to use for the bar fill. Default: \code{"steelblue"}.}

\item{line.color}{String indicating the color to use for the line and the dots of the cumulative curve. Default: \code{"navyblue"}.}

\item{title}{String indicating the title of the plot (markdown annotation supported). Default: \code{NULL}.}
}
\value{
A ggplot object.
}
\description{
Plots the proportion of protein variance carried by each component of a sPLS-DA, with the cumulative curve on top.
}
\details{
A sPLS-DA component is built to separate the classes, not to capture variance, and it only sees the handful of proteins retained by \code{keepX}. The percentages are therefore much lower than the ones of a PCA computed on the same data, and comparing the two is meaningless: what this plot shows is how the (little) variance used by the model is spread over the components.
}
\examples{
splsda <- perform.sPLSDA(DEprot.object = DEprot::test.toolbox$dpo.imp,
                         group.column = "condition",
                         keepX = 5,
                         validate = FALSE)

plot.sPLSDA.cumulative(DEprot.sPLSDA.object = splsda)

}
\seealso{
\link{perform.sPLSDA}, \link{plot.PC.cumulative}
}
\author{
Sebastian Gregoricchio
}
