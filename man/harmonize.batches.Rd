% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonize.batches.R
\name{harmonize.batches}
\alias{harmonize.batches}
\title{harmonize.batches}
\usage{
harmonize.batches(
  DEprot.object,
  batch.column = "batch",
  algorithm = "ComBat",
  ComBat.mode = 1,
  block = NULL,
  cores = 1,
  verbose = TRUE
)
}
\arguments{
\item{DEprot.object}{A \code{DEprot object}, as generated by \link{load.counts2}. Notice that this function works only with "raw" counts.}

\item{batch.column}{String indicating the name of a column of the metadata table in which are stored the batch IDs. Default: \code{"batch"}.}

\item{algorithm}{Adjustment method handed to \code{HarmonizR}. Either \code{"ComBat"} or \code{"limma"} (limma's \code{removeBatchEffect}). Default: \code{"ComBat"}}

\item{ComBat.mode}{Chooses the ComBat parameter set. Applied only when
\code{algorithm = "ComBat"} and ignored under limma. Each number is a
combination of ComBat's \code{par.prior} (parametric vs. non-parametric
empirical priors) and \code{mean.only} (adjust the mean alone, or both mean
and variance):
\itemize{
  \item \code{1} — parametric priors; adjusts location and scale
    (\code{par.prior = TRUE}, \code{mean.only = FALSE}).
  \item \code{2} — parametric priors; adjusts the mean only, variance left
    as is (\code{par.prior = TRUE}, \code{mean.only = TRUE}).
  \item \code{3} — non-parametric priors; adjusts location and scale, with
    no Gaussian assumption on the batch effect but slower to run
    (\code{par.prior = FALSE}, \code{mean.only = FALSE}).
  \item \code{4} — non-parametric priors; adjusts the mean only
    (\code{par.prior = FALSE}, \code{mean.only = TRUE}).
}
Default: \code{1}}

\item{block}{Number of batches grouped together during blocking. Larger blocks
yield fewer sub-matrices and cut runtime; \code{NULL} disables blocking.
Default: \code{NULL}.}

\item{cores}{Numeric value indicating the number of cores to use for the batch correction. Default: \code{1}.}

\item{verbose}{Logical value indicating whether processing messages should be printed. Default: \code{TRUE}.}
}
\value{
An object of class \code{DEprot}
}
\description{
The function uses the package \href{https://www.bioconductor.org/packages/release/bioc/html/HarmonizR.html}{\code{HarmonizR}} to correct batch effects in a combined table of multiple experiments.
}
\examples{
# Add batch column to the DEprot object
dpo = DEprot::test.toolbox$dpo.raw
dpo@metadata$batch = c(rep("A",6), rep("B",6))

# Correct batch effects
dpo <- harmonize.batches(DEprot.object = dpo,
                         batch.column = "batch",
                         cores = 1)


}
\author{
Sebastian Gregoricchio
}
